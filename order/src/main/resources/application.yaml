server:
  port: 8060

spring:
  application:
    name: order
management:
  otlp:
    metrics:
      export:
        enabled: false
  endpoints:
    web:
      exposure:
        include: health

kitchen-service:
  url: http://localhost:8070

delivery-service:
  url: http://localhost:8060
