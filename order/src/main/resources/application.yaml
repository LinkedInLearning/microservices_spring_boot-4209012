server:
  port: 8060
  # Problem Details without Stacktrace
  error:
    include-stacktrace: never

spring:
  application:
    name: order

resilience4j:
  ratelimiter:
    instances:
      rateLimiterApi:
        limit-for-period: 5
        limit-refresh-period: 60s
