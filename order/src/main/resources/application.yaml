server:
  port: 8060

spring:
  application:
    name: order
management:
  endpoints:
    web:
      exposure:
        include: health,metrics

kitchen-service:
  url: http://localhost:8070

delivery-service:
  url: http://localhost:8060
